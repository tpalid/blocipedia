<h3> <%= @wiki.title %> Collaborators </h3>
<% @user_collaborators.each do |user| %>
    <%= user.name %>: <%= user.email %></br>
<% end %>

<% if false %>
 <% if @wiki.user == current_user %>
        <%= link_to "Delete", wiki_collaborator_path(user), method: :delete, data: {confirm: 'Are you sure you want to delete this collaborator?' } %>
    <% end %>
<% end %>